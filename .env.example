DATABASE_PATH=nuclear.db
NRC_API_KEY=
STREAMLIT_PORT=8501
APP_PASSWORD=nuclear2024

# Scheduler (hours between runs, default 168 = weekly)
SCHEDULER_INTERVAL_HOURS=168

# Notifications: "log", "email", or "slack"
NOTIFY_METHOD=log

# Email settings (if NOTIFY_METHOD=email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
NOTIFY_EMAIL_FROM=
NOTIFY_EMAIL_TO=

# Slack webhook (if NOTIFY_METHOD=slack)
SLACK_WEBHOOK_URL=

# REST API
API_HOST=0.0.0.0
API_PORT=5000
API_KEY=
